name: 'Test Report'
on:
  workflow_run:
    workflows: ['CI']
    types:
      - completed

permissions:
  contents: read
  actions: read
  checks: write

jobs:
  report:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          #- macos-latest TODO

    runs-on: ubuntu-latest
    steps:
    - uses: dorny/test-reporter@v1
      with:
        artifact: test-results-${{ os.matrix }}
        name: Tests - {{ os.matrix }}
        path: '*.xml'
        reporter: java-junit